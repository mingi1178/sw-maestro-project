# Upstage Solar API key. Copy this file to `.env` and replace the value.
UPSTAGE_API_KEY=up_...

# Optional. Defaults are already set in `src/lib/server/upstage.ts`.
UPSTAGE_MODEL=solar-pro3
UPSTAGE_BASE_URL=https://api.upstage.ai/v1/solar
