# 5/4(월) 비동기 시작 체크리스트

> **회의 없음** — 각자 본인 페이스로 EOD까지 통과. 막히면 팀 채널 / GitHub Issue.
> 자료: 루트 `CLAUDE.md`, 같은 디렉토리의 `dev_plan.md`·`plan.md`, `docs/planning/people/<본인>.md`

## 0. 환경 셋업 (전원, ~30분)

- [ ] 레포 clone
- [ ] `python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt`
- [ ] `cp .env.example .env` 후 `OPENAI_API_KEY` 채우기
- [ ] `uvicorn backend.main:app --reload` → `http://localhost:8000/health` 200 OK 확인
- [ ] `pytest` 통과 확인
- [ ] (A·B) Flutter SDK 설치 확인 (`flutter --version`)

## 1. 슬라이스 분담 (확정 — 회의 불필요)

| 슬라이스 | 한 줄 책임 | 담당 |
|---|---|---|
| **A** | Flutter Web 프론트 (대시보드) | **노준영** |
| **B** | Chat UI + Agent 통신 프로토콜 | **박장우** |
| **C** | LangGraph Agent (prompt + graph + memory) | **이유준** |
| **D** | CRUD Tool (calendar + workouts) + **Tech Lead** | **박영준** |
| **E** | CRUD Tool (health) + scenarios 5개 적재 + 프롬프트 튜닝 주도 | **신승민** |

상세 본인 to-do는 `docs/planning/people/<본인>.md`.

## 2. 인터페이스 (이미 락 완료)

세 가지가 이미 코드/문서에 박혀 있음. 변경 시에만 `[interface-change]` PR + 5명 react.

- **Pydantic 모델**: `schemas/models.py` (9종 — `CalendarEvent` ~ `ChatChunk`)
- **Tool 시그니처**: `tools/CLAUDE.md` (`get_/create_/update_/delete_*` × 3 도메인)
- **REST 엔드포인트**: `backend/api/data.py`, `backend/api/chat.py` (FastAPI Swagger `/docs`에서 확인)
- **SSE 청크 포맷**: `schemas/CLAUDE.md` `ChatChunk.type`별 payload 표

## 3. LangGraph Quickstart (선택, 비동기)

LangGraph가 처음이면 [공식 Quickstart](https://langchain-ai.github.io/langgraph/) 1회 완주 권장.
C(이유준)는 필수 — 그래프·노드·체크포인터 컨셉을 깊게 이해해야 함.
A·B는 가벼운 개념(StateGraph, Tool 노드)만 봐도 OK.

## 4. 첫 PR 만들기 (각자, EOD까지)

각자 본인 슬라이스의 stub `NotImplementedError` / `501`을 **빈 함수가 더미를 반환**하도록 바꿔서 첫 PR 머지.

- [ ] **A**: `cd frontend && flutter create .` → 빈 Flutter Web 화면 1회 띄우기 → PR
- [ ] **B**: `frontend/lib/chat/` 디렉토리 + 채팅 위젯 골격 PR (입력창 + 메시지 리스트만)
- [ ] **C**: `agent/prompts.py`에 ReAct 3단계 강제 시스템 프롬프트 초안 PR (`run_agent_stream`은 이미 stub 동작)
- [ ] **D**: `data/calendar.json` 더미 5건 + `tools.get_calendar` JSON 파싱 1차 구현 PR
- [ ] **E**: `data/health.json` 더미 5건 + `tools.get_health` JSON 파싱 1차 구현 PR

## 5. 운영 룰 (확정)

- **합의 메커니즘**: 회의 없음. 인터페이스 변경은 `[interface-change]` PR + 5명 react.
- **공유 채널**: 팀 카톡/디스코드 (URL 합의 후 본 문서에 채울 것)
- **GitHub 레포 URL**: `______` (Tech Lead D가 채울 것)
- **PR 리뷰 turnaround**: 영업시간 4시간 권장 (오버나잇 OK)
- **Tech Lead D 박영준의 일일 점검**: 매일 EOD에 main 브랜치가 부팅되는지 확인 + 막힌 PR 있으면 팀 채널에 핑

## 6. 5/4 EOD 체크 (각자)

- [ ] 내 슬라이스의 첫 PR이 main에 머지되어 있다
- [ ] `uvicorn backend.main:app --reload`로 부팅, `GET /health` 200 OK
- [ ] `pytest` 통과
- [ ] (A) `flutter run -d chrome`으로 Flutter Web 빈 화면 떠 있다

## 다음 마일스톤

- 5/5 EOD: 각 슬라이스가 더미 입력으로 단독 실행 가능
- 5/8 EOD: ★ 1차 통합, Flutter ↔ FastAPI ↔ Agent end-to-end 1회 성공
- 5/10 EOD: 코드 동결, 데모 시나리오 무사고 시연
