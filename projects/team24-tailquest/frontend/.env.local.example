# Frontend environment template
# Copy to .env.local and edit.

# Set to "false" to call the real backend at /api/backend/*
# Default is true — uses mock data so the UI is usable without the backend.
NEXT_PUBLIC_USE_MOCK=true
